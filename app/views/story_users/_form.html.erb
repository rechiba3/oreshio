<%= form_for(@story_user) do |f| %>
  <% if @story_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story_user.errors.count, "error") %> prohibited this story_user from being saved:</h2>

      <ul>
      <% @story_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
